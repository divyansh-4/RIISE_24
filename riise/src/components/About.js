import React from 'react';
import { Link } from 'react-router-dom';
import './About.css'; // Import the CSS file for the header styles

const About = () => {
    return (
        <div>
            <h1>ABOUT</h1>
        </div>
    );
}

export default About;